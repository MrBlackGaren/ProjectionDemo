<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/demo_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="虚拟屏演示"
        android:textSize="36sp"
        android:textColor="#FF0000"
        android:fontFamily="sans-serif-bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/demo_content" />

    <TextView
        android:id="@+id/demo_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="这是在虚拟屏上运行的演示Activity\n\n功能：\n- 在虚拟屏上显示独立内容\n- 与主屏幕内容分离\n- 可用于演示不同的应用场景"
        android:textSize="18sp"
        android:textColor="#333333"
        android:lineSpacingExtra="8sp"
        android:padding="20dp"
        app:layout_constraintTop_toBottomOf="@id/demo_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/demo_button" />

    <Button
        android:id="@+id/demo_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="演示按钮"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:background="#4CAF50"
        android:paddingHorizontal="30dp"
        android:paddingVertical="15dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/demo_content" />

</androidx.constraintlayout.widget.ConstraintLayout>